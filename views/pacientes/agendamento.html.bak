{{extend 'layout.html'}}
<div class="row">
	<h3>Detalhes do agendamento</h3>
</div>
<div class="row">
	<p><strong>Nome:</strong> <a href="{{=URL('paciente', args=paciente.id)}}">{{=paciente.nome}}</a></p>
	<p><strong>Dia agendado:</strong> {{=agendamento.dia}}</p>
	<p><strong>Hora início:</strong> {{=agendamento.hora_inicio}}</p>
	<p><strong>Hora fim:</strong> {{=agendamento.hora_fim}}</p>
</div>
	<div class="row">
		<div class="col-md-12">
			<a href="{{=URL('editar_cadastro', args=paciente['id'])}}" class="btn btn-default" role="button"><span class="icon pen icon-pencil glyphicon glyphicon-pencil">Editar</span></a>
			<a href="{{=URL('agendar', args=paciente['id'])}}" class="btn btn-default" role="button"><span class="icon plus icon-plus glyphicon glyphicon-plus">Apagar</span></a>
		</div>
	</div>
</div>
<div class="row">
	{{=form}}
</div>
